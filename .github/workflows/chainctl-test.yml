name: Chainctl Test

on:
  workflow_dispatch: {}

jobs:
  assume-and-explore:
    name: actions assume example

    permissions:
      id-token: write

    runs-on: ubuntu-latest
    steps:
#obtained identity with
# chainctl iam identities create gc-github-actions \
# --identity-issuer="https://token.actions.githubusercontent.com" \
# --subject-pattern="garrettcrosby/images-diff-test:(ref:refs/heads/main)" \
# --role=registry.pull \
# --parent=garrett.crosby
    - uses: chainguard-dev/setup-chainctl@main
      with:
        identity: "8db1c02f42c500d4c158df58938ea71dbe2d3c12/06111c3b3c0c6e52"

    - run: |
        chainctl images repos list --parent garrett.crosby
